{
  "name": "melektron",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "start": "next start"
  },
  "dependencies": {
    "next": "14.2.30",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "5.11.4"
  }
}
"scripts": {
  "build": "next build && next export",
  "postbuild": "cp robots.txt ./out/robots.txt"
}
"scripts": {
  "dev": "next dev",
  "build": "next build && next export", // Generiši statički sajt
  "start": "next start"
}
{
  "scripts": {
    "dev": "next dev -H 0.0.0.0",
    "build": "next build",
    "start": "next start -H 0.0.0.0",
    "repl": "npm run dev"
  }
}
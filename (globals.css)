/* Kvantni efekti za blog */
.quantum-blog-post {
  position: relative;
  overflow-x: hidden;
}

.quantum-particle-background {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(
    ellipse at center,
    rgba(10, 10, 24, 1) 0%,
    rgba(5, 5, 15, 1) 100%
  );
  z-index: -1;
}

.quantum-particle-background::before {
  content: '';
  position: absolute;
  width: 200%;
  height: 200%;
  top: -50%;
  left: -50%;
  background-image: 
    radial-gradient(white 1px, transparent 2px),
    radial-gradient(cyan 1px, transparent 2px),
    radial-gradient(purple 1px, transparent 2px);
  background-size: 300px 300px;
  background-position: 0 0, 100px 100px, 200px 200px;
  opacity: 0.05;
  animation: quantum-drift 120s infinite linear;
}

@keyframes quantum-drift {
  100% {
    transform: rotate(1deg) translate(50px, 25px);
  }
}

.quantum-post-card {
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.quantum-post-card:hover {
  transform: translateY(-5px) scale(1.02);
  box-shadow: 0 20px 30px rgba(0, 0, 0, 0.3);
}